apiVersion: 1

datasources:
- name: ad_prom
  type: prometheus
  access: proxy
  url: "http://localhost:9090"
  password:
  user:
  database:
  basicAuth: false
  isDefault: true
  editable: true
  jsonData:
  readOnly: false
  version: 1
